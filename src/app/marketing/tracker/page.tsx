"use client";

import React from "react";

export default function MarketingTrackerPage(): JSX.Element {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="max-w-2xl text-center">
        <h1 className="text-2xl font-semibold">Marketing Tracker (temporary)</h1>
        <p className="mt-2 text-sm text-slate-400">Temporary placeholder page to unblock build. Restore full implementation from git history.</p>
      </div>
    </main>
  );
}
